# 物价折价改造项目全量分析报告（修订版）

> 分析日期：2026-05-11  
> 范围：`src/`、`his-client/`、`sql/`、`docs/`、根目录文档

## 结论

当前仓库已经不是“纯设计稿”，而是有完整 C# 源码、测试和 SQL 的可运行工程。  
我已验证：

- `dotnet build src\Pricing.RuleCenter.slnx --no-restore` 成功
- `dotnet test src\Pricing.RuleCenter.slnx --no-restore` 成功，25/25 通过

但代码、SQL、文档之间仍有几处必须优先修的缺口，主要集中在：

1. 数据库初始化不完整
2. 规则匹配入口不完整
3. 批量/子项/追溯链路不完整
4. 文档和代码的接口约定不一致
5. 缓存失效和动作顺序种子不一致

## 1. 代码/文档遗漏

1. `PR_RULE_APPROVAL`、`PR_RULE_TEST_CASE`、`PR_RULE_TEST_RUN` 的仓储已经写了，但 `sql/01-create-tables.sql` 没有对应建表和序列，部署后相关插入会直接失败。  
   参考：`src/Pricing.RuleCenter.Infrastructure/Repositories/RuleApprovalRepository.cs:107-115`，`RuleTestCaseRepository.cs:100-108`，`RuleTestRunRepository.cs:86-94`

2. 文档里有 `POST /api/pricing/calculate/batch-simulate`，但代码里没有这个独立端点。现在只是 `simulate/confirm` 支持一次传多个 `Items`，不是文档写的那个路径。  
   参考：`docs/物价折价改造方案文档/05-统一计价接口设计.md:163-171`，`src/Pricing.RuleCenter.Api/Controllers/PricingController.cs:121-305`

3. `PricingCalculateRequest` 和 `BuildContext` 没有把 `VisitType`、`PatientAge`、`ItemGroupCode` 接进来，但核心引擎已经支持这些条件。结果是 `VISIT_TYPE_MATCH`、`AGE_MATCH` 以及部分组规则在正式接口上没有稳定输入。  
   参考：`src/Pricing.RuleCenter.Api/Dto/PricingDto.cs:1-150`，`src/Pricing.RuleCenter.Api/Services/PricingApiService.cs:776-804`，`src/Pricing.RuleCenter.Core/Models/PricingContext.cs:61-103`

4. `AddChildItemExecutor` 只把子项写进 `PricingContext.ChildPricingResults`，但 `PricingApiService` 只持久化了 `ReplaceChildResult` 那条 REPLACE 链路。普通子项加收不会进入 API 返回，也不会落到折价明细。  
   参考：`src/Pricing.RuleCenter.Core/Engine/Executors/AddChildItemExecutor.cs:101-105`，`src/Pricing.RuleCenter.Api/Services/PricingApiService.cs:970-1161`

## 2. 不明确的地方

1. `GroupMatchEvaluator` 会在匹配阶段修改 `context.ItemGroupCode`，这让“匹配”和“执行”之间出现了隐式副作用。后续 `SameGroupMutexExecutor`、`SameOperationCeilingExecutor` 都会依赖这个副作用，链路不够直白。  
   参考：`src/Pricing.RuleCenter.Core/Engine/Evaluators/GroupMatchEvaluator.cs:98-132`

2. `SameOperationCeilingExecutor` 依赖 `context.ItemGroupCode`，但 `BuildContext` 不直接装载这个字段。它能不能生效，取决于前面有没有走到 `GROUP_MATCH`。  
   参考：`src/Pricing.RuleCenter.Core/Engine/Executors/SameOperationCeilingExecutor.cs:118-138`，`src/Pricing.RuleCenter.Api/Services/PricingApiService.cs:776-804`

3. `RuleHeaderService.GetEffectiveAsync(itemCode)` 先按 `DateTime.Now` 取全量生效规则，再做 `itemCode` 过滤。这个接口到底是“按当前时间预览”还是“按指定时间回放”，文档和实现没有统一。  
   参考：`src/Pricing.RuleCenter.Api/Services/RuleHeaderService.cs:149-171`

## 3. 文档与代码不一致

1. `CLAUDE.md` 和 `AGENTS.md` 还写着“暂无源代码 / 设计阶段”，但仓库里已经有 `src/`、`tests/`、`his-client/` 全套源码。这个说明已经过期。  
   参考：`CLAUDE.md:9`，`AGENTS.md:5`

2. `05-统一计价接口设计.md` 写的是 `GET /api/pricing/rules/effective?itemCode=xxx&chargeTime=yyy`，但代码只有 `itemCode`，而且内部固定用 `DateTime.Now`。  
   参考：`docs/物价折价改造方案文档/05-统一计价接口设计.md:136`，`src/Pricing.RuleCenter.Api/Controllers/RuleHeaderController.cs:122-145`

3. 同一份接口文档还写了 `POST /api/pricing/calculate/batch-simulate`，代码没有这个路由。  
   参考：`docs/物价折价改造方案文档/05-统一计价接口设计.md:163-171`

4. `DictSeedData.cs` 已经定义了 `ACTION_TYPE_ORDER`、`MUTUALLY_EXCLUSIVE_ACTION_TYPE` 和 `SAME_OPERATION_CEILING`，但 `sql/02-init-dict-data.sql` 没有把 `ACTION_TYPE_ORDER` 这套种子真正落库。  
   结果是：如果数据库没手工补种子，`RuleMatchService` 就会回退到代码默认顺序，而默认顺序里还没有 `SAME_OPERATION_CEILING`。  
   参考：`src/Pricing.RuleCenter.Infrastructure/Database/DictSeedData.cs:57-86`，`src/Pricing.RuleCenter.Core/Engine/RuleMatchService.cs:90-101`，`sql/02-init-dict-data.sql`

5. `ActionExecutionPipeline.GetStepType` 没把 `SAME_OPERATION_CEILING` 和 `ADD_CHILD_ITEM` 映射到明确的追溯步骤类型，最后会落成 `VALIDATE`，这和动作语义不一致。  
   参考：`src/Pricing.RuleCenter.Core/Engine/ActionExecutionPipeline.cs:186-204`

## 4. 设计缺陷与架构缺陷

1. `PricingApiService` 太重，已经同时承担了校验、幂等、事务、持久化、冲正、特殊标识查询等职责，是典型的 god service。后面继续加业务，只会越来越难维护。  
   参考：`src/Pricing.RuleCenter.Api/Services/PricingApiService.cs`

2. `RuleMatchService` 只按 `ITEM_CODE` 取候选规则：  
   `var candidates = await _headerRepository.GetByItemCodeAsync(context.ItemCode);`  
   这使得 `GROUP` 范围规则不是第一类入口，更多像“靠条件侧边补救”，架构上不稳。  
   参考：`src/Pricing.RuleCenter.Core/Engine/RuleMatchService.cs:167-169`，`src/Pricing.RuleCenter.Infrastructure/Repositories/RuleHeaderRepository.cs:107-112`

3. 生效规则缓存只删了 `rules:effective:all`，没删按 `itemCode` 存的缓存键。发布、停用、回滚之后，带 `itemCode` 的旧结果会继续命中到 TTL 结束。  
   参考：`src/Pricing.RuleCenter.Api/Services/RuleHeaderService.cs:141-187`，`src/Pricing.RuleCenter.Api/Services/RulePublishService.cs:660-664`

4. 规则动作顺序、字典种子、默认回退顺序三套东西没有完全对齐。这个问题会直接影响限额、同组互斥、同手术封顶、超额归零的执行顺序，属于资金风险点，不是纯风格问题。  
   参考：`src/Pricing.RuleCenter.Core/Engine/RuleMatchService.cs:90-101`，`src/Pricing.RuleCenter.Infrastructure/Database/DictSeedData.cs:57-86`

## 5. 当前可用性

- 核心 API 已实现：simulate、confirm、commit、cancel、reverse、special-flag
- 追溯、限额、冲正、规则发布链路已经成型
- 25 个测试全部通过

但如果按“医院真实上线”标准看，上面列的几个缺口仍然不能忽略，尤其是：

- 数据库初始化
- 批量/子项链路
- `GROUP`/年龄/就诊类型条件
- 缓存失效
- 接口文档和实际路由一致性

