# 折价模块代码评审报告（PricingController 计价链路）

> 评审日期：2026-06-14
> 评审范围：`PricingController` 暴露的 8 个接口及其完整折价计价链路
> 评审方法：自入口控制器逐层下钻，实读 Workflow、计价引擎、折价执行器、持久化、额度并发、DTO 契约与状态常量，核心结论均有代码佐证（标注 `文件:行`）。

---

## 0. 评审范围与已读文件

`PricingController` 是一个极薄的入口层，8 个端点各自委托给一个应用服务：

| 接口 | 委托对象 |
|------|----------|
| `POST calculate/simulate` | `PricingSimulateWorkflow` |
| `POST calculate/confirm` | `PricingConfirmWorkflow` |
| `POST calculate/commit` | `PricingCommitWorkflow` |
| `POST calculate/cancel` | `PricingCancelWorkflow` |
| `POST calculate/reverse` | `PricingReverseWorkflow` |
| `GET items/{itemCode}/special-flag` | `PricingSpecialFlagResolver` |
| `POST items/special-flags` | `PricingSpecialFlagResolver.ResolveBatchAsync` |
| `POST items/special-flags/any` | `PricingSpecialFlagResolver.ResolveAnyAsync` |

下钻实读：5 个 Workflow、`PricingSpecialFlagResolver`、`PricingEngine`、`RuleActionPlanBuilder`、`ExpressionFormulaExecutor`、`ExceedToZeroExecutor`、`AmountFloorExecutor`、`AmountCeilingExecutor`、`OnceQtyLimitExecutor`、`TimeWindowLimitExecutor`、`LimitExecutionSupport`、`PricingContextFactory`、`PricingConfirmationPersistenceService`、`FormulaExpressionValidator`、`FormulaEvaluationContext`、`StatusCodes`、计价请求/响应 DTO；额度锁、负向占用、过期清理通过子代理实读核实。

---

## 1. 总体评价

**整体质量优秀，属于经过认真设计的金融级实现。** 架构分层清晰、设计模式运用到位、资金安全意识强（防死锁加锁顺序、滑动窗口全桶锁定、负向占用、三表状态同步、未知动作类型拒绝静默放行）。代码可读性高，注释充分。

主要问题集中在三类：
1. **一处配置可触发的资金风险**（公式可绕过数量限额，P1）；
2. **若干"靠巧合维系"的脆弱耦合**（动作类型字面量、状态常量双体系，P1/P2）；
3. **与团队 .NET 规范的偏离**（超长方法/类、重复代码、注释语言不一致，P2/P3）。

没有发现会立即导致线上事故的确定性 Bug；但上述脆弱耦合在后续演进中存在"被无意改动后静默超收/漏算"的隐患，建议优先收敛。

---

## 2. 架构设计评估 —— 优

### 2.1 分层与职责（符合三层规范）
- `Core`（模型/接口/枚举/常量/值对象）→ `Infrastructure`（仓储/DB）→ `Application`（Workflow/Engine/Executor）→ `Api`（薄 Controller），依赖方向正确。
- **Controller 极薄**：仅做 `[FromServices]` 方法级注入 + 委托 + 包 `ApiResult`，无业务逻辑，符合"Controller 不承载逻辑"的预期。
- **计价引擎与 I/O 解耦**：`PricingEngine` 明确声明"不写库、不处理 HTTP 幂等"（`PricingEngine.cs:13-16`），只负责"上下文→规则匹配→动作链→金额"，幂等/锁/事务全部留在 Workflow，边界干净。
- **DTO↔领域边界清晰**：`PricingContextFactory` 把 snake_case、请求级/明细级分层的 HTTP DTO 规范化为单条费用 `PricingContext`，统一 trim、空串转 null（`PricingContextFactory.cs:74-117`），让 evaluator/executor 不再关心原始报文结构。

### 2.2 设计模式（符合 OCP）
- **条件-动作分离 + 执行器模式**：`IRuleActionExecutor` / `IRuleConditionEvaluator` / `ILimitOccupyValueFinalizer` 均为策略集合，新增规则类型只需新增执行器。`PricingEngine.ApplyFinalOccupyValues`（`PricingEngine.cs:198-219`）按占用类型选择唯一 finalizer，找不到唯一匹配即抛异常——不静默兜底，资金安全意识好。
- **全局动作顺序集中化**：`RuleActionPlanBuilder.DefaultActionTypeOrder`（`RuleActionPlanBuilder.cs:31-44`）把"资金口径顺序"收敛到单点，并支持 `PR_DICT` 覆盖；未登记动作类型直接抛异常而非排到末尾继续收费（`:174-184`）。这是正确的"配置缺陷即停"取向。

### 2.3 生命周期与可追溯
三阶段确认 `confirm → commit → cancel` + `reverse` 状态机完整，每个阶段都"请求日志 + 折价明细 + 限额占用"三表一起变更（如 `PricingCancelWorkflow.cs:74-77`、`PricingCommitWorkflow.cs:97-100`），追溯链与额度口径一致。

---

## 3. 业务逻辑正确性核对

| 校验项 | 结论 | 佐证 |
|--------|------|------|
| 计价计算顺序（CLAUDE.md 强制） | ✅ 完全一致 | `DefaultActionTypeOrder` 顺序：换算→日限→时间窗→单次→同组互斥→公式→金额下限→金额上限→同手术封顶→子项加收→超出归零，与规范逐项吻合 |
| confirm 幂等 | ✅ 健壮 | 双重检查锁（事务外预读 + 事务内加锁后重读，`PricingConfirmWorkflow.cs:68-103`）+ 指纹比对 + 响应快照重放 |
| 滑动时间窗 2 小时 | ✅ 正确 | 枚举窗口覆盖的**全部小时桶**并逐一加锁（`TimeWindowLimitExecutor.cs:95-110`，注释举例 10:01 覆盖 08/09/10） |
| 并发额度锁 | ✅ 正确 | `MERGE` 占位行 + `SELECT … FOR UPDATE` 行锁，按 `Ordinal` 排序加锁防死锁 |
| 数量截断同步缩金额 | ✅ 正确 | `LimitExecutionSupport.ApplyRemainingQty` 截断 `FinalQty` 时按比例缩 `FinalAmount`，中间不取整（`LimitExecutionSupport.cs:13-38`） |
| 超限基准 | ✅ 正确 | 超限以 `ConvertedQty` 而非 `InputQty` 为基准，正确处理双单位换算（`ExceedToZeroExecutor.cs:73-79`） |
| 退费 | ✅ 健壮 | 负向占用符号为负、结果组完整性校验、最后一组承接差额防 decimal 截断（`PricingReverseWorkflow.cs:298-315`） |
| 中间精度/最终取整 | ✅ 正确 | 中间不取整，最终 `PricingAmountRounder.RoundFinal` 统一 2 位四舍五入 |
| NULL≠0 语义 | ✅ 正确 | 金额上下限参数用 `decimal?` 区分"未配置"与"配置为 0"（`AmountFloorExecutor.cs:99` 注释明确） |

---

## 4. 关键风险与缺陷（按优先级）

### P1 —— 应优先处理

#### P1-1　公式可绕过前置数量限额，存在配置可触发的多收费风险 ★核心
- **现象**：`FormulaEvaluationContext` 的变量白名单暴露了 `inputQty` 与 `originalAmount`（`FormulaEvaluationContext.cs:47,59`），其中 `originalAmount = UnitPrice × InputQty`（`ExpressionFormulaExecutor.cs:102`，基于**未截断**的原始数量）。`FormulaExpressionValidator` 只校验"变量是否在白名单内"，不限制使用哪些变量（`FormulaExpressionValidator.cs:30-50`）。
- **链路**：动作顺序中 `FORMULA_CALC(5)` 在数量限额 `DAY(1)/TIME_WINDOW(2)/ONCE(3)` 之后；公式执行时 `context.FinalAmount = amount` **直接覆盖**（`ExpressionFormulaExecutor.cs:82-88`）。若规则配置写成 `originalAmount * 0.8` 或 `inputQty * unitPrice * 0.8`，则金额基于未截断量计算；此时 `FinalQty` 已被限额截断（部分超限 `FinalQty>0`），而 `ExceedToZeroExecutor` 的 ZERO 模式仅在 `FinalQty<=0` 时归零（`ExceedToZeroExecutor.cs:141-149`），**无法纠正部分超限**。
- **后果**：超出限额部分仍按原始量收费，直接违反 CLAUDE.md「公式不自动跳过限制」「有数量限制时公式使用限制后的 FinalQty」「超出部分→0 元」三条强约束。
- **建议**（任选）：
  - A. 从公式白名单移除 `inputQty`、`originalAmount`，或让 `originalAmount` 在公式阶段重算为 `unitPrice × finalQty`，强制公式只能基于限额后量；
  - B. `FormulaExpressionValidator` 增加规则：同项目/同规则存在数量限额动作时，禁止表达式引用 `inputQty`/`originalAmount`；
  - C. 动作链末尾增加"`FinalAmount` 与 `FinalQty` 一致性"后置校验。

#### P1-2　占用状态写入使用双常量体系，靠"值巧合"维系
- **现象**：占用表（`PR_LIMIT_OCCUPY`）状态在不同路径用了不同常量类：
  - confirm 持久化写 `OccupyStatusCodes.Pending`；reverse 写 `OccupyStatusCodes.Reversed`；
  - 但 **commit 写 `BusinessStatusCodes.Confirmed`**（`PricingCommitWorkflow.cs:99-100`）、cancel 写 `BusinessStatusCodes.Cancelled`；
  - 而限额查询用**字面量数组** `{ "PENDING", "CONFIRMED" }`（`OnceQtyLimitExecutor.cs:27`、`TimeWindowLimitExecutor.cs:25`）。
- **当前为何不出错**：`BusinessStatusCodes.Confirmed == OccupyStatusCodes.Confirmed == "CONFIRMED"`（`StatusCodes.cs:131,173`），值恰好相同。
- **风险**：这是脆弱耦合。一旦有人调整 `BusinessStatusCodes.Confirmed` 的字面值（注释已写明历史曾用 `COMMITTED`，`StatusCodes.cs:134-137`）或改动查询数组，commit 后的占用将查不到 → 后续限额累计漏计 → **超收**。
- **建议**：占用表一律使用 `OccupyStatusCodes`；限额查询的 `OccupyStatuses` 改用常量引用而非字面量。

#### P1-3　动作类型编码大量使用裸字符串字面量，缺乏单一来源
- **现象**：常量类 `RuleActionTypeCodes`（位于 `RuleCapabilityCodes.cs:102`）**已定义全部动作类型常量**，发布校验侧（`RuleActionParameterValidator`、`RuleConflictDetector`、`RuleCriticalActionGuard`）全部使用常量；但**运行期执行器侧**除公式族用了 `RuleActionTypeCodes.FormulaCalc` 外，其余 10 个执行器的 `ActionType` 与 `RuleActionPlanBuilder.DefaultActionTypeOrder` 字典 key 全部是裸字面量（`AmountCeilingExecutor.cs:34` `"APPLY_MAX_AMOUNT"`、`ExceedToZeroExecutor.cs:55` `"DISCOUNT_EXCEED_TO_ZERO"`、`OnceQtyLimitExecutor.cs:46`、`TimeWindowLimitExecutor.cs:43` 等）。
- **风险**：这些字面量必须与排序字典 key 严格一致，否则 `GetActionTypeSortOrder` 抛"未登记动作类型"异常（`RuleActionPlanBuilder.cs:182`）。常量明明已存在却不用，任何拼写漂移都只能在运行期暴露，编译期无法发现。
- **建议**：全部执行器 `ActionType` 与 `DefaultActionTypeOrder` 改引用 `RuleActionTypeCodes`。

### P2 —— 应处理

#### P2-1　过期清理无分页/批量上限
- 后台 `ExpireCleanupService` 调用 `GetPendingExpiredAsync`（`ChargeRequestLogRepository.cs:261`）**无 Take/分页**，一次性 `ToListAsync` 后逐条独立事务。挂起记录积压时单轮可能过载、长时间占用连接。
- **建议**：加 `Take(batchSize)` 分批处理，单轮上限可配置。

#### P2-2　锁过期时间硬编码，与确认过期解耦
- 锁过期硬编码 10 分钟（`EnsureAndLockAsync` 内），未走配置，与 `PricingOptions.ConfirmExpireMinutes` 相互独立，存在不一致风险（确认未过期但锁已过期）。
- **建议**：纳入 `PricingOptions`，与确认过期口径对齐。

#### P2-3　业务收费时间缺少强制校验
- `PricingCalculateRequest.BusinessChargeTime` 是非空 `DateTime` 但**无 `[Required]`/非默认值校验**（`PricingCalculateDto.cs:64-65`）。调用方不传时得到 `DateTime.MinValue`，将污染规则生效判断与滑动窗口累计。业务时间是 CLAUDE.md「业务时间优先」的核心字段。
- **建议**：在 `PricingRequestGuard` 校验 `BusinessChargeTime` 非默认值（明细级缺失时回退根级，根级也缺失则拒绝）。

#### P2-4　超长方法/类，偏离团队规范（方法≤30 行、类<300 行）
- `PricingReverseWorkflow`（385 行）：`ExecuteAsync` 内联 lambda ~75 行、`ValidateResultGroupIntegrity` ~60 行、`BuildReverseContextAsync` ~55 行。
- `PricingSpecialFlagResolver`（513 行）：`ResolveBatchAsync` ~107 行。
- **建议**：拆分退费编排（锁定/幂等/可退校验/分摊/落库各成一法），下沉分摊算法到独立类（详见 P2-5）。

#### P2-5　special-flag 批量与 any 大段重复代码
- `ResolveBatchAsync`（`:121-156`）与 `ResolveAnyAsync`（`:231-264`）中"逐行规范化 + 维度合并 + 构造 `SpecialFlagRequest`"几乎逐字重复。
- **建议**：抽取 `BuildSingleRequest(item, request)` 复用，消除 DRY 违规。

### P3 —— 建议/打磨

- **P3-1 CallType 字面量散落**：`"SIMULATE"`/`"CONFIRM"`/`"SPECIAL_FLAG"` 等散落在 Workflow 与 `PricingContextFactory`；`PricingConfirmWorkflow` 多处用 `"CONFIRM"` 字面量（`:67,99,155`），而 `PricingSimulateWorkflow` 已用常量 `PricingCallTypeCodes.Simulate`。建议统一用 `PricingCallTypeCodes`（常量已存在）。
- **P3-2 注释一致性**：`PricingConfirmationPersistenceService.cs:43-51` 为英文 XML 注释（全项目其余为中文）；`TimeWindowLimitExecutor.cs:10-11` 类摘要是"负责执行一种可配置动作"的模板占位文案，不如其他执行器具体；`ExceedToZeroExecutor.cs:157` 注释提到的 `PricingApiService` 已不存在（实际是 `PricingDiscountDetailWriter`）。
- **P3-3 兼容字段较多**：`MinAmount/FloorAmount`、`MaxAmount/CeilingAmount`、`MaxOnceQty/LimitQty/MaxQty`、`WindowHours/WindowMinutes/LimitQty/MaxQty` 等多套同义字段。历史 74 条规则迁移期可理解，但建议迁移完成后统一清洗，运行期只认推荐字段，减少长期维护面。
- **P3-4 响应 DTO 字段冗余**：`PricingCalculateResponse` 同时有根级 `FinalAmount/DiscountAmount/UnitPrice/FinalQty`、`TotalFinalAmount/TotalDiscountAmount/TotalOriginalAmount` 与 `Items[]` 三套口径（`PricingResponseDto.cs:53-92`），注释自陈"语义等同兼容字段"。调用方易误用。建议明确主用 `items[]`，根级标注 deprecated 并给迁移期限。
- **P3-5 幂等响应快照的 legacy snake_case 兜底**：`PricingConfirmWorkflow.cs:195-197` 的 `RewritePropertyNamesToSnakeCase` 属历史兼容 hack，确认历史数据消化后可移除。
- **P3-6 疑似未使用注入**：`PricingConfirmWorkflow` 注入的 `_clock`、`_options` 在当前可见实现路径中未见使用（过期时间在持久化服务内计算），建议核对后移除。
- **P3-7 静态缓存影响可测试性**：`RuleActionPlanBuilder` 用 `static s_actionTypeOrderCache`/`s_cacheLoaded`（`:49,60`），跨实例、跨测试共享状态，单测之间会相互污染（一个用例加载字典后其余用例 `s_cacheLoaded==true` 直接跳过）。建议改为单例服务持有实例级缓存或 `IMemoryCache`。

---

## 5. 与团队 .NET 规范符合度

| 规范条目 | 符合度 | 说明 |
|----------|--------|------|
| 三层架构、Core 无外部依赖 | ✅ | 分层清晰 |
| `sealed` 默认 | ✅ | Workflow/Executor/DTO 均 sealed |
| 构造函数注入、无 service locator | ✅ | 全程构造注入 |
| `Result` 模式而非异常表达业务错误 | ⚠️ | 计价链路统一用 `BizException`（含 HTTP code）表达业务错误，与"Result 模式"取向不同；属团队内部需统一的取舍，非缺陷 |
| 结构化日志 | ✅ | 全部 `LogInformation("…{Param}", param)`，无字符串插值 |
| `IReadOnlyList<T>` 返回集合 | ✅ | 一致使用 |
| 金额用 `decimal` | ✅ | DTO/上下文/执行器全程 decimal |
| 方法≤30 行、类<300 行、参数≤4 | ⚠️ | 见 P2-4（退费/特殊标识解析超标） |
| OCP：新行为=新类 | ✅ | 执行器/finalizer/evaluator 策略化 |
| 异步方法 `Async` 后缀 + `CancellationToken` | ⚠️ | `Async` 后缀齐全；但计价链路普遍**未透传 `CancellationToken`**，长事务下无法及时取消，建议补充 |

---

## 6. 亮点（值得保持）

1. **资金口径集中且"配置缺陷即停"**：全局动作顺序单点收敛 + 未知动作类型抛异常 + finalizer 唯一性校验，杜绝静默放行。
2. **滑动窗口全桶锁定**：正确识别窗口跨小时边界并锁定全部桶，避免并发突破 2 小时限额。
3. **退费分摊防截断**：多结果组按比例分摊时"最后一组承接差额"，保证各组之和恒等于总退费，体现了对 decimal 精度的敏感。
4. **幂等三件套**：业务号锁 + 请求指纹比对 + 响应快照重放，超时重试安全。
5. **公式沙箱**：表达式只能访问受控数值白名单，不能触达患者/项目对象，且发布前试运行校验语法——安全取向正确（白名单内容需按 P1-1 收窄）。

---

## 7. 处置建议（投入产出排序）

1. **先做 P1-1**：收窄公式变量白名单 / 增加发布校验——唯一有直接资金后果的项，改动小、收益高。
2. **再做 P1-2、P1-3、P3-1**：把状态码、动作类型、调用类型统一到既有常量——纯机械替换，显著降低后续演进事故面。
3. **接着 P2-1 / P2-3**：过期清理分批 + 业务时间强制校验——稳定性与口径正确性。
4. **最后 P2-4 / P2-5 与 P3 其余**：拆分超长方法、消重、注释统一、CancellationToken 透传——可在迭代中渐进完成。

> 备注：本报告所有结论基于 2026-06-14 当前代码静态评审；P1-2 的"值巧合一致"等判断如后续状态常量调整，请同步复核。
