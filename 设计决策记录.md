# 设计决策记录

> 决策日期：2026-05-11

---

## D1：同胎次限制

**方案：** HIS 通过 `ExtraParams` 传入胎次标识，计价引擎按胎次分组累计。

**字段定义：**
- `ExtraParams["pregnancyId"]` — string，胎次唯一标识（如 "PREG_001"），同一次孕产的所有检查共享同一标识
- `ExtraParams["twinType"]` — string，胎型编码（"SINGLE"/"TWINS"/"TRIPLETS"），来自项目名称后缀

**执行逻辑：**
- 评估器按 `pregnancyId` 分组，同组内同类项目累计
- 超过限制数量的部分归零（不是拒单）

**扩展性：** 未来如需按"同孕次"扩展，只需新增 `pregnancyNo` 字段到 ExtraParams，评估器逻辑不变。

---

## D2：皮瓣移植同一手术封顶

**方案：** HIS 通过 `ExtraParams` 传入手术标识，计价引擎按手术+项目组维度累计封顶。

**字段定义：**
- `ExtraParams["operationId"]` — string，手术单号或手术唯一标识
- 规则动作 `ParamsJson` 中配置 `ceilingPerOperation`（同一手术封顶金额）和 `ceilingPerItem`（单条封顶金额）

**执行逻辑：**
- 同一 `operationId` 下，同项目组的金额累计不超过 `ceilingPerOperation`
- 单条明细金额不超过 `ceilingPerItem`
- 两级封顶：先检查单条封顶，再检查手术封顶

**扩展性：** 未来新增"同手术"类限制，只需复用 `operationId` 维度，无需修改数据结构。

---

## D3：单位换算系数来源

**方案：** 换算系数维护在规则动作的 `ParamsJson` 中，支持按部位区分。

**ParamsJson 结构：**
```json
{
  "convertRules": [
    { "bodyPartCode": "HEAD_FACE", "inputUnit": "CM2", "outputUnit": "COUNT", "divisor": 4, "roundMode": "CEILING" },
    { "bodyPartCode": "TRUNK", "inputUnit": "CM2", "outputUnit": "COUNT", "divisor": 144, "roundMode": "CEILING" },
    { "bodyPartCode": "BODY_SURFACE", "inputUnit": "PERCENT", "outputUnit": "COUNT", "divisor": 1, "roundMode": "CEILING" }
  ],
  "defaultDivisor": 1,
  "defaultRoundMode": "CEILING"
}
```

**执行逻辑：**
- 按 `bodyPartCode` 匹配换算规则
- `换算数量 = ⌈输入数量 / divisor⌉`（向上取整）
- 无匹配时使用 `defaultDivisor`

**扩展性：** 新增项目只需配置 ParamsJson，无需新增执行器代码。支持任意输入/输出单位组合。

---

## D4：rollbackMode 回滚边界

**方案：** 默认 `STOP_CHARGE`（暂停收费转人工），最安全。

**枚举值：**
- `STOP_CHARGE` — 暂停收费，转人工处理（默认值，资金安全优先）
- `LEGACY_EQUIVALENT` — 自动切回旧计价逻辑（仅特殊项目经审批后使用）
- `MANUAL_REVIEW` — 继续使用新服务但标记需人工复核

**实现位置：** `PR_RULE_HEADER` 表新增 `ROLLBACK_MODE` 字段，默认 `STOP_CHARGE`。

**回滚触发条件：**
- 计价服务不可用时，`STOP_CHARGE` 项目禁止收费
- `LEGACY_EQUIVALENT` 项目自动切回旧逻辑
- `MANUAL_REVIEW` 项目继续计价但结果标记待复核

---

## D5：批量试算策略

**方案：** 第一阶段逐项处理 + 同批共享累计上下文。

**实现：**
- `SimulateAsync` 接收 Items 列表，逐项调用引擎
- 同批内共享 `Dictionary<string, decimal>` 记录已占用的数量/金额（按 LimitDimension 分组）
- 后续项目计算时，先查询同批内已占用量，再查询数据库历史占用量

**扩展性：** 未来如需完整 BatchPricingContext（同组互斥跨项目判断），只需将 Dictionary 替换为 BatchContext 对象。

---

## D6：LIMIT_KEY 统一生成器

**方案：** 抽取 `LimitKeyGenerator` 静态工具类，统一所有限制类型的 KEY 生成逻辑。

**格式规范：**
- 日数量：`DQ:{patientId}:{itemCode}:{yyyyMMdd}`
- 日金额：`DA:{patientId}:{itemCode}:{yyyyMMdd}`
- 单次：`OQ:{sourceSystem}:{businessRequestNo}:{itemCode}`
- 时间窗：`TW:{patientId}:{itemCode}:{yyyyMMddHH}`
- 同手术：`OP:{operationId}:{itemGroupCode}`
- 同胎次：`PG:{pregnancyId}:{itemGroupCode}`

---

## D7：缓存层

**方案：** IMemoryCache 进程内缓存，第一阶段足够。

**缓存对象：**
- 生效规则列表（TTL 5分钟，发布/停用时主动失效）
- 字典数据（TTL 30分钟）
- 公式定义（TTL 30分钟）

**失效策略：** 规则发布/停用/回滚时主动清除规则缓存。
