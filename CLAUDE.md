# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目概述

这是一个**物价折价改造项目**的设计与规划仓库。目标是将医院信息系统（HIS）中硬编码的物价/折价规则，改造为可配置、可维护的统一计价规则中心，服务于所有渠道（HIS、自助机、微信）。

项目当前处于**设计阶段**，暂无源代码。仓库包含需求文档、架构设计、数据库表设计草案、接口规范和任务拆解。

## 技术栈

- **运行时：** .NET 6
- **数据库：** Oracle 11g（无原生JSON支持，用CLOB；无自增，用SEQUENCE）
- **ORM：** SqlSugarCore
- **接口：** ASP.NET Core Web API
- **驱动：** Oracle.ManagedDataAccess.Core
- **前端：** HIS系统（C# Windows Forms/WPF）
- **金额类型：** C# 始终使用 `decimal`，Oracle 使用 `NUMBER(18,4)` —— 禁止使用 `double` 或 `float`

## 架构

三大核心组件：

1. **规则维护中心** — 物价规则的增删改查、字典维护、公式定义、规则版本管理与生命周期
2. **统一计价引擎** — 规则匹配、双单位换算、公式执行、金额/数量上下限、折价计算
3. **折价追溯中心** — 规则变更审计、计价请求日志、计算步骤日志、折价结果明细

### 计划项目结构

```
Pricing.RuleCenter.Api           — HTTP接口层
Pricing.RuleCenter.Application   — 用例、事务、编排
Pricing.RuleCenter.Domain        — 规则匹配、公式计算、折价决策
Pricing.RuleCenter.Infrastructure— SqlSugarCore、Oracle、日志、缓存
Pricing.RuleCenter.Contracts     — 请求/响应DTO、枚举、接口契约
```

### 核心设计模式

- **条件-动作分离：** 规则建模为"满足条件时执行动作"（PR_RULE_CONDITION + PR_RULE_ACTION）
- **执行器模式：** `IRuleConditionEvaluator`、`IRuleActionExecutor`、`IPricingFormulaExecutor` —— 新增规则类型只需新增执行器，无需修改各渠道代码
- **三阶段确认：** `confirm`（占用额度）→ `commit`（HIS结算）→ `cancel`（释放额度）
- **可追溯性作为架构原则：** 每笔折价必须可沿三条链路追溯——规则变更链、计算过程链、最终结果链

## 核心业务规则

### 计价计算顺序（强制）

1. 按项目、场景、部位、时间匹配规则
2. 双单位换算（如已配置）
3. 公式计算（如已配置）
4. 金额下限
5. 金额上限
6. 日数量限制
7. 时间窗数量限制（如2小时窗）
8. 同组互斥 / 同手术封顶
9. 子项加收 / 附加项目
10. 超出部分 → 0元（不是整单，不是拒单）

### 关键业务约束

- **全院累计：** 按收费途径统计，范围包括门诊收费、手术/医技划价、终端确认、护士过医嘱/分解医嘱；已退费记录不计入累计
- **单次口径：** “单次”指单次收费动作，包括门诊收费保存、手术/医技划价、终端确认、护士过医嘱/分解医嘱；“单条”指单条收费明细
- **特殊项目口径：** “折价项目”和“特殊项目”是同一标识，仅命名不同
- **多规则边界：** 同一项目允许按不同部位维护不同换算规则；不允许同一项目维护不同折价公式或不同折价额度规则
- **退费额度：** 退费按退费数量释放额度；当日退费释放数量，隔日退费重收后按重收当天重新做额度校验；2 小时规则按收费时间（包含重收时间）向前查 2 小时
- **金额取整：** 中间计算保留全部精度；最终金额保留 2 位小数，四舍五入
- **超出 = 0元：** 不是"拒单"，不是"整单归零"，仅超出部分为0元
- **NULL ≠ 0：** NULL 表示"不校验"，0 表示"限制为零"。前端空值必须存为 NULL
- **公式优先于限制：** 公式 + 金额限制共存时，先算公式，再与限制比较
- **公式不自动跳过限制：** 公式项目是否执行数量、时间窗、同组、同手术限制，以规则动作配置为准；禁止代码层一见公式就跳过全部数量类限制
- **换算数量固定为1：** `换算数量`是固定值；公式使用换算后数量，非输入数量
- **模拟 vs 确认：** 试算不占用额度，仅确认计价才占用
- **业务时间优先：** 规则生效、2 小时窗口、单日累计必须使用业务收费发生时间，不得使用技术占用时间替代
- **复杂输入明细化：** 多肿物、多部位、多面积项目必须使用 `pricingParts` 明细表达，不能压成单个数量粗算

## 接口端点

| 方法 | 路径 | 用途 |
|------|------|------|
| POST | `/api/pricing/calculate/simulate` | 试算（不占额度） |
| POST | `/api/pricing/calculate/confirm` | 确认计价（占用额度） |
| POST | `/api/pricing/calculate/commit` | HIS结算成功通知 |
| POST | `/api/pricing/calculate/cancel` | 取消确认（释放额度） |
| POST | `/api/pricing/calculate/reverse` | 退费/冲销 |
| GET | `/api/pricing/items/{itemCode}/special-flag` | 查询是否特殊项目 |
| POST | `/api/pricing/trace/query` | 追溯查询 |
| GET | `/api/pricing/rules/effective` | 查询生效规则 |

## 数据库表（PR_前缀）

**规则配置：** PR_RULE_HEADER, PR_RULE_VERSION, PR_RULE_CONDITION, PR_RULE_ACTION, PR_FORMULA_DEF, PR_DICT

**项目分组：** PR_ITEM_GROUP, PR_ITEM_GROUP_DETAIL

**审核与发布：** PR_RULE_PUBLISH, PR_RULE_CHANGE_LOG, PR_RULE_APPROVAL

**计价追溯：** PR_CHARGE_REQUEST_LOG, PR_CHARGE_TRACE_STEP, PR_CHARGE_DISCOUNT_DETAIL

**并发控制：** PR_LIMIT_OCCUPY, PR_LIMIT_LOCK（SELECT ... FOR UPDATE 模式）

**冲销：** PR_CHARGE_REVERSE_LOG

**测试：** PR_RULE_TEST_CASE, PR_RULE_TEST_RUN

## 文档索引

需求与参考资料位于仓库根目录和 `docs/`：

| 文件 | 用途 |
|------|------|
| `需求描述.txt` | 原始需求简述 |
| `物价界面改造1——标识及上下限.txt` | 原始需求提出折价页签、标识、数量/金额限制 |
| `物价界面改造2——计价公式.txt` | 公式计价、公式与限制的交互 |
| `物价界面改造3——双单位.txt` | 双单位换算 |
| `docs/物价折价规则整理.xlsx` | 74条历史规则，需迁移 |
| `docs/医保立项指南服务改造方案.pptx` | 更广泛的业务背景 |
| `11-落地行动计划.md` | 需求澄清清单、HIS现状摸底、业务口径确认结果 |
| `12-历史规则分类与公式定义.md` | 74条历史规则分6族、公式定义、换算规则 |
| `13-开发批次计划.md` | 12个开发批次、每批次交付物和验证标准 |

设计文档在 `docs/物价折价改造方案文档/`，按 01-13 顺序阅读。最低必读：01、02、03、04、05、08、09、10、11、13。

特别注意：
- `08-设计缺陷与补充方案.md` — 25 个设计缺陷与补充方案，5 个 P0 级问题必须在编码前解决
- `10-资金风险补齐方案.md` — 幂等、滑动窗口、状态机、批量试算、主子项目、回滚边界等资金安全硬约束，必须在收费计算服务编码前解决
- `04-Oracle表设计草案.md` — 已合并 08 的补充方案（LIMIT_KEY 规则、互斥组、版本一致性、滑动窗口等）
- `05-统一计价接口设计.md` — 已合并 08 的补充方案（批量试算、分页、超时降级、SLA 等）
- `09-HIS集成与部署方案.md` — HIS 特殊计价规则维护工作台、收费弹窗、灰度发布、性能容量、上线检查清单

## 稳定性要求（金融级）

- **幂等性：** confirm 接口必须幂等（键：`sourceSystem + businessRequestNo + callType`），并保存 `REQUEST_FINGERPRINT`；超时重试必须复用同一业务号
- **单次限额维度：** 单次限额按单次收费动作统计，维度应使用 `sourceSystem + businessRequestNo + itemCode` 或同等收费动作键；不能按单条收费明细号替代
- **并发额度：** PR_LIMIT_LOCK + SELECT FOR UPDATE 防止多渠道限额突破；TIME_WINDOW 必须锁定业务时间窗口覆盖的全部小时桶
- **规则冲突校验：** 同一项目允许不同部位存在不同换算规则；同一项目、同一场景、同一生效期内不允许多套折价公式、金额上下限或额度规则同时生效
- **状态一致：** confirm、commit、cancel、expire、reverse 必须同时更新请求日志、限额占用、折价明细状态
- **退费释放：** 当日退费按退费数量释放额度；部分退费必须校验本次退费 + 历史已退不超过原有效收费；隔日退费重收后按重收当天重新校验额度
- **权威单价：** confirm 不得直接信任渠道传入 `unitPrice`，必须由计价中心读取权威单价或强校验，不一致返回 `PRICE_MISMATCH`
- **批量试算：** batch-simulate 必须共享 `BatchPricingContext`，不能把同组互斥、同胎次、同手术项目按完全独立项目计算
- **主子项目：** 子项加收必须与主项目同 `resultGroupNo` 原子 commit/cancel
- **规则发布：** 发布前必须校验规则重叠、动作组冲突、重复子项目、缺少测试用例等阻断项
- **取整策略：** 中间金额不提前取整；最终对外金额和落账金额统一保留 2 位小数、四舍五入；实现必须统一且可配置（roundMode、roundScale、minChargeQty、ceilBaseQty）
- **特殊项目：** 计价服务不可用时，渠道不得回退为普通计价
- **回滚边界：** 只有 `rollbackMode = LEGACY_EQUIVALENT` 的特殊项目允许自动切回旧逻辑；其他项目必须转人工、暂停或继续使用新服务
- **缓存失效：** 规则发布/停用/回滚时必须立即失效缓存
- **挂起清理：** 后台任务必须扫描长时间处于 CONFIRM_PENDING 状态的记录

## 实施优先级

1. 数据模型设计（规则表 + 追溯表）
2. 资金安全补齐设计（幂等、状态机、窗口锁、单价校验、回滚边界）
3. 工作台内置字典维护（计价类型、单位、公式类型）
4. HIS 特殊计价规则维护工作台
5. 物价计算服务（核心引擎）
6. 追溯与审计
7. 收费录入集成（特殊项目弹窗）
8. 历史规则迁移（xlsx中74条规则）
9. 每日对账与异常报表
10. 测试与部署

## 已确认与待确认问题（实现前必须确认）

已确认：
- "单次"指单次收费动作；"单条"指单条收费明细。
- "折价项目"和"特殊项目"是同一标识。
- 一个项目允许按不同部位维护不同换算规则；不允许维护不同折价公式或不同折价额度规则。
- 退费按退费数量释放额度；当日退费释放数量，隔日退费重收后按重收当天重新做额度校验。
- 中间金额保留全部精度；最终金额保留 2 位小数，四舍五入。

待确认：
- 返还额度的具体实现方式仍需彬哥和收费处确认，现有开发逻辑主要关注重收时校验。
- "同手术""同孕次"标识在现有系统中的来源
- HIS 能否提供稳定的 `businessRequestNo`，以及 confirm 超时重试是否能复用
- 权威物价单价从 HIS 哪张表或同步表读取，价格版本如何追溯
- 多肿物、多部位项目收费入口是否能采集 `pricingParts`
- 每个特殊项目的 `rollbackMode` 是否已确认
