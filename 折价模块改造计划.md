# 折价模块改造计划（P1~P3 全量处理）

> 来源：`折价模块代码评审报告.md`
> 工作流：直接提交 `main` 并推送到 `origin`（`ymign/PriceDiscountTransformation`）
> 提交粒度：**每批次一次**（构建 + 测试通过后 commit & push）
> 处理原则：项目未上线、无兼容包袱，做**彻底重构**，不保留兼容字段 / legacy 兜底 / TODO 占位
> 测试策略：先修复测试基线（批次 0），之后每批改动同步补充 / 修正相关测试用例，保证回归护栏
> 每完成一项即在本文件勾选 `[x]`，每批次完成后追加提交哈希。

## 进度总览

- 总计：15 项（+ 批次 0 测试基线）　已完成：批次 0 已就绪　进行中：批次 1
- [x] 批次 0：修复测试基线（编译 + 全绿：Core 38/38、Tests 359/359）
- [ ] 批次 1：P1 资金安全与正确性（3 项）
- [ ] 批次 2：P2 稳定性与契约校验（3 项）
- [ ] 批次 3：P2 结构重构（2 项）
- [ ] 批次 4：P3 常量统一与打磨（4 项）
- [ ] 批次 5：P3 破坏性清理（3 项）

---

## 批次 0 —— 修复测试基线（计划外既有债务）

> 现状：src 生产代码编译通过，但两个测试工程编译失败（测试落后于一次重构）。先修复使其编译通过并全绿，作为后续每批回归护栏。

- [x] **B0-1 补全 fake 仓储 `GetByItemCodesAsync`**
  - 实现：在 `IRuleHeaderRepository` 提供默认接口实现（逐个委托 `GetByItemCodeAsync`），一处消除全部 14 个 fake 的缺失实现。
- [x] **B0-2 迁移旧命名空间/类型到 `Engine.EffectiveRules`**
  - `EffectiveRuleSnapshotLoader`→`EffectiveRuleReader`、`EffectiveRuleSnapshotCache`→`EffectiveRuleViewCache`、`.Snapshots`→`.Rules`、`using ...RuleRuntimeSnapshot`→`...EffectiveRules`。
  - 删除测已移除功能的死用例（`BatchSimulateAsync` 路由、`RuntimePackage*` 实体字段断言），适配过时断言（`RuleSnapshotTime`→`RuleReadTime`、`rule_snapshot_time`→`rule_read_time`）。
- [x] **B0-3 两个测试工程编译通过并全绿**
  - 结果：Core.Tests 38/38、Tests 359/359 全部通过。

---

## 批次 1 —— P1 资金安全与正确性

> 目标：消除唯一有直接资金后果的风险，并把"靠巧合维系"的脆弱耦合收敛到常量。

- [ ] **P1-1 公式不得绕过前置数量限额**
  - 位置：`FormulaEvaluationContext.cs`、`ExpressionFormulaExecutor.cs`、`FormulaExpressionValidator.cs`
  - 动作：从公式可见白名单移除 `inputQty`、`originalAmount`（或令 `originalAmount` 在公式阶段重算为 `unitPrice × finalQty`），强制公式只能基于限额后量；同步更新校验器试算上下文与相关单测。
- [ ] **P1-2 占用状态统一用 `OccupyStatusCodes`**
  - 位置：`PricingCommitWorkflow.cs`、`PricingCancelWorkflow.cs`、`OnceQtyLimitExecutor.cs`、`TimeWindowLimitExecutor.cs`、`DailyQtyLimitExecutor.cs`
  - 动作：占用表写入与查询一律使用 `OccupyStatusCodes` 常量，消除 `BusinessStatusCodes` 交叉写入与 `{"PENDING","CONFIRMED"}` 字面量。
- [ ] **P1-3 动作类型编码统一用 `RuleActionTypeCodes`**
  - 位置：10 个执行器的 `ActionType` + `RuleActionPlanBuilder.DefaultActionTypeOrder`
  - 动作：全部裸字符串替换为 `RuleActionTypeCodes` 常量，消除拼写漂移风险。

---

## 批次 2 —— P2 稳定性与契约校验

- [ ] **P2-1 过期清理分页 / 批量上限**
  - 位置：`ExpireCleanupService.cs`、`ChargeRequestLogRepository.GetPendingExpiredAsync`
  - 动作：增加可配置 `Take(batchSize)` 分批扫描，避免积压时单轮全量加载。
- [ ] **P2-2 锁过期时间走配置**
  - 位置：`LimitLockRepository`/`LimitOccupyRepository`、`PricingOptions`
  - 动作：锁过期分钟数纳入 `PricingOptions`，与 `ConfirmExpireMinutes` 口径对齐。
- [ ] **P2-3 业务收费时间强制校验**
  - 位置：`PricingRequestGuard`、`PricingCalculateDto`
  - 动作：校验 `BusinessChargeTime` 非默认值（明细缺失回退根级，根级也缺失则拒绝）。

---

## 批次 3 —— P2 结构重构

- [ ] **P2-4 拆分超长方法 / 类**
  - 位置：`PricingReverseWorkflow.cs`（385 行）、`PricingSpecialFlagResolver.cs`（513 行）
  - 动作：退费编排拆分（锁定 / 幂等 / 可退校验 / 分摊 / 落库各成一法），分摊算法下沉独立类；方法尽量 ≤30 行、类 <300 行。
- [ ] **P2-5 special-flag 批量 / any 消重**
  - 位置：`PricingSpecialFlagResolver.ResolveBatchAsync` / `ResolveAnyAsync`
  - 动作：抽取 `BuildSingleRequest(item, request)` 复用逐行规范化与维度合并逻辑。

---

## 批次 4 —— P3 常量统一与打磨

- [ ] **P3-1 CallType 字面量统一 `PricingCallTypeCodes`**
  - 位置：5 个 Workflow、`PricingContextFactory`
  - 动作：`"SIMULATE"/"CONFIRM"/"SPECIAL_FLAG"` 等改用常量。
- [ ] **P3-2 注释一致性**
  - 位置：`PricingConfirmationPersistenceService`（英文 XML）、`TimeWindowLimitExecutor`（模板占位摘要）、`ExceedToZeroExecutor`（过时 `PricingApiService` 引用）
  - 动作：统一中文、补具体业务摘要、修正过时引用。
- [ ] **P3-6 移除未使用注入**
  - 位置：`PricingConfirmWorkflow`（`_clock`、`_options`）
  - 动作：核对后移除未使用依赖。
- [ ] **P3-7 静态缓存改实例级 / 单例**
  - 位置：`RuleActionPlanBuilder`（`s_actionTypeOrderCache`、`s_cacheLoaded`、`s_cacheLock`）
  - 动作：改为单例服务持有实例级缓存或 `IMemoryCache`，消除跨实例 / 测试共享状态。

---

## 批次 5 —— P3 破坏性清理（未上线，彻底删除兼容）

- [ ] **P3-3 兼容字段清洗**
  - 位置：各执行器参数模型（`MinAmount/FloorAmount`、`MaxAmount/CeilingAmount`、`MaxOnceQty/LimitQty/MaxQty`、`WindowHours/WindowMinutes/LimitQty/MaxQty` 等）
  - 动作：只保留推荐字段，删除旧字段及其解析分支。
- [ ] **P3-4 响应 DTO 字段冗余清理**
  - 位置：`PricingResponseDto`（根级 `FinalAmount/DiscountAmount/UnitPrice/FinalQty` vs `TotalXxx` vs `items[]`）
  - 动作：以 `items[]` + `TotalXxx` 为唯一口径，移除根级单项目兼容字段。
- [ ] **P3-5 移除 legacy 快照兜底**
  - 位置：`PricingConfirmWorkflow`（`RewritePropertyNamesToSnakeCase`）、`RuleCenterJsonSerializer`
  - 动作：删除 snake_case 重写兜底，幂等快照只认当前格式。

---

## 提交记录

| 批次 | 提交信息 | 哈希 | 状态 |
|------|----------|------|------|
| 0 | | | 待提交 |
| 1 | | | 待提交 |
| 2 | | | 待提交 |
| 3 | | | 待提交 |
| 4 | | | 待提交 |
| 5 | | | 待提交 |
